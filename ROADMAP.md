# General Mini Agent Framework 路线图

`0.1.0` 稳定范围仅包含 OpenAI 兼容模型上的单 Agent 同步工具调用。路线图只记录尚未
稳定或仍需实现的能力；当前稳定能力以 [README.md](README.md) 为准。

## 0.2：流式执行

目标：让流式路径与 `0.1.0` 同步路径具备一致、可测试的协议语义。

- 根据 tool call index 聚合多个流式工具调用
- 处理分片 JSON 参数、不完整响应和异常结束原因
- 对齐同步与流式的 trace、usage、hook 和停止原因
- 定义公开流式事件类型并补齐回归测试

## 0.3：记忆与上下文

目标：将当前实验性记忆组件发展为可配置、可隔离的上下文策略。

- 明确 Agent 执行后的消息写回契约
- 为消息对和 tool call 边界增加窗口裁剪测试
- 增加 Token 预算裁剪和历史摘要策略
- 定义长期记忆写入、检索和会话命名空间
- 抽象可替换向量存储并支持元数据过滤

## 0.4：多 Agent

目标：稳定参与者协作、Judge 裁决和多次运行隔离。

- 分离参与者轮次与 Judge 最终裁决
- 让 `max_rounds` 和收敛判断真实生效
- 每次运行建立独立共享上下文
- 覆盖无 Judge、提前收敛、异常角色和重复运行

## 后续：工具与模型扩展

- 增加异步模型与 Agent 接口
- 支持工具超时、取消和执行权限
- 支持结构化工具返回值
- 为模型服务能力差异增加兼容层
- 增加统一配置加载、校验和日志接口

## 后续：编排与可观测性

- 增加串行、并行和条件路由编排
- 提供统一事件模型和运行 ID
- 为 trace 增加耗时、错误和模型请求摘要
- 支持 JSON trace 导出
- 为 HTML 报告增加过滤和对比能力

## 后续：示例与发布

- 增加离线 Mock Demo
- 补充 API 文档和常见问题
- 建立变更日志和发布验证
- 整理各稳定版本对应的最小示例

每个版本稳定后同步更新 README，并将对应实验性标记移除。规划中的能力不得描述为
当前稳定功能。
