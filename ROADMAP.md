# General Mini Agent Framework 路线图

当前稳定版本为 `1.9.0`。已经交付的能力以 [README.md](README.md) 和
[CHANGELOG.md](CHANGELOG.md) 为准；本文只记录尚未完成的工作。

## 1.9.x 稳定化

补丁版本只处理缺陷、兼容性、跨平台差异和文档收口，不新增公开能力。

- 验证受控命令执行在 Python 3.12/3.13 与 Linux、Windows、macOS 上的行为一致性
- 加强超时后的进程树清理和异常路径诊断
- 保持同步、流式、异步和多 Agent 路径的状态与事件契约一致
- 扩充长期记忆、并行调度和工具隔离的压力与故障测试

## 强化执行隔离

Phase 1 只提供 cwd、环境变量、超时和有界输出捕获，不是不受信任代码的安全边界。
后续隔离后端需要明确的平台能力检测，并在无法满足请求时 fail-closed。

- 文件系统与网络隔离
- CPU、内存、磁盘和进程数量限制
- 容器或独立执行服务边界
- 可测试、结构化的资源限制与隔离错误
- 评估未来主版本是否默认启用受限执行

## 可观测性与运行治理

- OpenTelemetry 集成
- 模型调用、工具执行、记忆访问和工作流节点之间的关联追踪
- 速率限制、重试和取消事件的统一观测契约
- 不泄漏密钥、命令环境或后端异常细节的日志策略

## 工程化

- 可复现的开发与发布环境
- wheel/sdist 安装契约和可选依赖矩阵
- 公共 API 类型检查与兼容性检查
- 更复杂的异步编排能力在稳定边界明确后再引入

## 规划约束

- 公共 API 破坏性变更只进入主版本
- 新能力需要同步、流式或异步路径的明确支持范围
- Agent 实例之间的状态、工具和上下文必须隔离
- 可选依赖继续延迟加载
- 文档不得把规划能力描述为当前实现

历史版本级任务拆解保留在 [docs/superpowers/plans](docs/superpowers/plans)。
