# General Mini Agent Framework 架构说明

## 项目目标

General Mini Agent Framework 提供一组结构简单、边界明确的 Agent 基础组件。项目优先保证执行过程可阅读、可调试、可替换，在此基础上逐步补充工程能力。

## 设计原则

1. **显式执行流程**：模型请求、工具调用、观察结果和最终答案均通过明确的数据结构传递。
2. **小型模块边界**：模型、工具、记忆、执行器、协作和轨迹渲染分别维护。
3. **兼容标准协议**：模型层使用 OpenAI 兼容的 Chat Completions 和 function calling 格式。
4. **可选外部组件**：长期记忆等重依赖采用延迟初始化，不影响基础 Agent 使用。
5. **失败可观察**：工具异常、迭代耗尽和请求失败应产生可定位的信息。
6. **行为优先于抽象**：只有在出现真实复用需求时才增加 Provider、Runtime 或 Pipeline 抽象。

## 模块边界

### `core/llm.py`

负责模型服务通信与协议转换：

- 管理模型地址、密钥、模型名称和生成参数
- 发送同步或 SSE 流式请求
- 解析文本、工具调用、结束原因和 Token 用量
- 处理超时、连接错误及可重试 HTTP 状态

该模块不执行工具，也不管理对话状态。

### `core/tools.py`

负责 Python 函数与模型工具协议之间的转换：

- 使用装饰器注册工具
- 根据函数签名生成 JSON Schema
- 提供工具查询、列表和执行入口
- 将执行结果转换为模型可消费的字符串

该模块不决定何时调用工具。

### `core/memory.py`

提供两类独立记忆：

- 滑动窗口记忆保存短期消息上下文
- ChromaDB 记忆保存可语义检索的长期文本

记忆组件只负责存储和读取，不负责生成摘要或决定保留策略。

### `core/agent.py`

负责单 Agent 执行循环：

```text
构建上下文
  -> 请求模型
  -> 判断是否调用工具
  -> 记录工具观察
  -> 继续请求或返回最终答案
```

同步和流式入口应保持一致的事件语义、工具行为和停止条件。

### `core/debate.py`

负责多个 Agent 的角色协作。编排层组织共享上下文和发言顺序，但不侵入单 Agent 的执行逻辑。

### `core/trace.py`

负责将运行结果转换为自包含 HTML。渲染层只消费结果对象，不参与 Agent 执行。

## 数据流

```text
User Input
    |
    v
Agent + Memory -----> LLM Client
    ^                    |
    |                    v
Tool Result <------ Tool Call
    |
    v
AgentResult -------> Trace Renderer
```

## 公共接口稳定性

当前公共入口集中在 `core/__init__.py`：

- `LLM`, `LLMConfig`, `LLMResponse`, `StreamChunk`
- `tool`, `Tool`, `ToolRegistry`
- `SlidingWindowMemory`, `LongTermMemory`
- `Agent`, `AgentResult`

后续修改应尽量保持这些名称和基础调用方式稳定。需要破坏兼容性时，应在变更说明中记录迁移方式。

## 错误处理

- 模型认证和非临时 HTTP 错误直接抛出。
- 超时、连接错误和部分服务端错误允许有限重试。
- 工具异常转换为观察文本，使 Agent 可以选择修正参数或结束任务。
- 达到最大迭代次数时返回明确的终止结果。
- 可选依赖缺失时，在首次使用对应组件时提供安装提示。

## 测试策略

单元测试优先使用模拟模型响应，避免依赖外部网络和真实 API Key。每个模块至少覆盖：

- 正常路径
- 空响应或无结果路径
- 无效参数和异常路径
- 多轮状态变化
- 同步与流式行为一致性

需要真实模型的验证放在独立集成测试中，并通过环境变量显式启用。

## 近期工程重点

具体迭代顺序见 [ROADMAP.md](ROADMAP.md)。近期优先处理状态闭环、Agent 工具隔离、流式多工具调用以及缺失的自动化测试。
