# General Mini Agent Framework 路线图

`0.3.0` 已稳定 OpenAI 兼容模型上的单 Agent 同步/流式工具调用、显式上下文预算、
确定性历史裁剪和内存会话写回。
路线图只记录尚未稳定或仍需实现的能力；当前稳定能力以 [README.md](README.md) 为准。

## 0.3.1：长期记忆

目标：在稳定会话契约上增加可替换、可隔离的持久化检索。

- 定义长期记忆写入、检索和会话命名空间
- 抽象可替换向量存储并支持元数据过滤
- 明确记忆选择、更新和遗忘策略
- 覆盖不同用户、会话和 Agent 实例的存储隔离

## 0.4：多 Agent

目标：稳定参与者协作、Judge 裁决和多次运行隔离。

- 分离参与者轮次与 Judge 最终裁决
- 让 `max_rounds` 和收敛判断真实生效
- 每次运行建立独立共享上下文
- 覆盖无 Judge、提前收敛、异常角色和重复运行

## 后续：工具与模型扩展

- 增加异步模型与 Agent 接口
- 支持工具超时、取消和执行权限
- 支持结构化工具返回值
- 为模型服务能力差异增加兼容层
- 增加统一配置加载、校验和日志接口

## 后续：编排与可观测性

- 增加串行、并行和条件路由编排
- 提供统一事件模型和运行 ID
- 为 trace 增加耗时、错误和模型请求摘要
- 支持 JSON trace 导出
- 为 HTML 报告增加过滤和对比能力

## 后续：示例与发布

- 增加离线 Mock Demo
- 补充 API 文档和常见问题
- 建立变更日志和发布验证
- 整理各稳定版本对应的最小示例

每个版本稳定后同步更新 README，并将对应实验性标记移除。规划中的能力不得描述为
当前稳定功能。
