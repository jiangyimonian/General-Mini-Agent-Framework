# General Mini Agent Framework 路线图

路线图只记录尚未完成或需要强化的工作。已存在的基础能力以 [README.md](README.md) 为准。

## Stage 1：执行正确性

目标：让同步、流式和多 Agent 路径在状态管理与工具行为上保持一致。

### 对话状态闭环

- 在每次 Agent 执行后写回用户消息和最终答案
- 明确工具调用消息是否进入长期对话上下文
- 为窗口裁剪增加成对消息和 tool call 边界测试

### 工具隔离

- 将 Agent 可用工具从全局注册表中隔离
- 保留装饰器注册的便捷用法
- 防止不同 Agent 之间出现同名工具覆盖或能力泄漏

### 流式协议补全

- 根据 tool call index 聚合多个流式工具调用
- 处理不完整 JSON 参数和异常结束原因
- 对齐同步与流式的 trace、usage 和 hook 行为

### Debate 控制流

- 分离参与者轮次与 Judge 最终裁决
- 让 `max_rounds` 和收敛判断真实生效
- 每次运行建立独立共享上下文
- 增加无 Judge、提前收敛和多次运行测试

## Stage 2：工程化基础

目标：让项目可以稳定安装、配置和验证。

- 增加 `pyproject.toml` 和锁定的开发环境
- 将运行依赖与测试依赖分组
- 增加统一配置加载和配置校验
- 增加日志接口，替代散落的终端输出
- 增加格式化、静态检查和测试命令
- 建立基础 CI 流程

## Stage 3：模型与工具扩展

目标：支持更多模型服务和受控工具场景。

- 抽象最小模型客户端协议
- 增加异步模型与 Agent 接口
- 支持工具超时、取消和执行权限
- 支持结构化工具返回值
- 增加请求级重试策略和错误分类
- 为模型服务能力差异增加兼容层

## Stage 4：记忆与上下文管理

目标：从固定消息窗口扩展到可配置的上下文策略。

- 增加 Token 预算裁剪
- 支持历史对话摘要
- 定义长期记忆写入策略
- 支持可替换的向量存储接口
- 增加记忆命名空间和会话隔离
- 为检索结果增加相关度阈值和元数据过滤

## Stage 5：编排与可观测性

目标：为复杂任务提供更可靠的执行和诊断能力。

- 增加串行、并行和条件路由编排
- 提供统一事件模型
- 为 trace 增加耗时、错误和模型请求摘要
- 支持 JSON trace 导出
- 增加运行 ID 与子任务关联
- 为 HTML 报告补充过滤和对比能力

## Stage 6：示例与发布

目标：形成可复用的项目入口和发布流程。

- 整理最小示例、工具调用示例和多 Agent 示例
- 增加离线 Mock Demo，避免演示依赖外部服务
- 补充 API 文档和常见问题
- 建立版本号和变更日志
- 增加打包与发布验证

## 优先顺序

1. 修复状态与工具隔离问题
2. 补齐流式和 Debate 测试
3. 建立可安装、可复现的开发环境
4. 再扩展异步、编排和多 Provider 能力

每个阶段完成后都应同步更新 README 的能力列表，并确保规划项不会被描述为已完成功能。
